[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn --chdir backend app:app --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
healthcheckPath = "/api/hotels"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3

[[services]]
name = "efoyta-web"
